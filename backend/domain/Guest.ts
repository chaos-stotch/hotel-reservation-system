class Guest {
  name: string;
  document: string;
  contact: string;

  constructor (name:string, document:string, contact:string) {
    this.name = name;
    this.document = document;
    this.contact = contact;
  }
}